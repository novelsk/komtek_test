# КОМТЕК - тестовое задание

Шаблон .env файла:
```bash
SECRET_KEY=django-insecure
DEBUG=1
```
### Запуск
```bash
# Запускать через:
pyton manage.py runserver localhost:8000
# Или
docker-compose up -d
```
### [Swagger](http://localhost:8000/api/schema/swagger-ui)
